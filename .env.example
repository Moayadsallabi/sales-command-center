# Copy to .env.local and fill in.

# Internal integration secret from https://www.notion.so/my-integrations (starts with ntn_ or secret_)
NOTION_API_KEY=

# The database's 32-char id, from its URL. Dashes optional.
NOTION_DATABASE_ID=

# Optional. Personal access token from calendly.com/integrations/api_webhooks.
# Adds the booking side of the funnel: what was booked, cancelled and no-showed,
# not just what got recorded. Leave blank and the dashboard runs on recordings
# alone, which is what it did before Calendly was supported.
CALENDLY_API_KEY=

# Which Calendly event types count as sales calls, comma-separated. Names as
# they read in Calendly; matching is case-insensitive and partial. Leave unset
# and EVERY booking counts, including one-to-ones and personal meetings.
# Example: Strategy Call,Discovery Call
#
# An entry starting with "!" EXCLUDES instead, and exclusions win. A list of
# only exclusions means "everything except these" — Brey runs "!onboarding".
#
# EITHER SHAPE DECIDES SILENTLY FOR ANYTHING NEW: a deny-list counts every new
# event type, an allow-list drops every new one. Both have cost real numbers
# here. `calendly-event-types.json` is the ledger of types a human has ruled
# on, and `npm run check:calendly` speaks up when the calendar holds one that
# is not in it. Keep that file current and this setting can be whatever suits.
CALENDLY_EVENT_TYPES=

# Optional. How far back to read bookings. Defaults to 90 days.
CALENDLY_LOOKBACK_DAYS=

# Optional. How long the booking list is reused before Calendly is asked again.
# Defaults to 300 seconds.
CALENDLY_CACHE_SECONDS=

# Optional, demo mode only. Set to 1 alongside DASHBOARD_DEMO_DATA=1 to preview
# the dashboard as it looks before Calendly is connected.
DEMO_WITHOUT_CALENDLY=

# The client's business name. Titles the dashboard header and names the
# footer. Leave blank and the header keeps the product name.
NEXT_PUBLIC_BRAND_NAME=

# The currency every total on the dashboard is shown in. Defaults to USD.
# Rows priced in something else are converted using that row's FX Rate, so set
# this to the currency you actually report in — it does not convert anything on
# its own, it decides which side of the conversion the totals land on.
NEXT_PUBLIC_REPORTING_CURRENCY=

# Login password. The dashboard REFUSES TO SERVE without this, because it
# renders prospect names, deal sizes and call summaries. To publish it with no
# login anyway, leave this blank and set DASHBOARD_PUBLIC=1 below.
DASHBOARD_PASSWORD=

# Basic-auth username. Defaults to "admin" when unset.
DASHBOARD_USER=

# Set to 1 to serve the dashboard with no login at all. Only with
# DASHBOARD_PASSWORD blank, and only if you mean it.
DASHBOARD_PUBLIC=

# Set to 1 to render invented sample calls instead of reading Notion, so you
# can see the dashboard before the first real call lands. Never in a real
# deployment.
DASHBOARD_DEMO_DATA=

# Optional. Payment processor key. Two things use it:
#   - the dashboard itself, on every page load, so Cash Collected shows what
#     the processor banked rather than what closers typed;
#   - `npm run check:payments`, which reconciles the tracker against money that
#     actually arrived — the only way a deal that closed three days after the
#     call gets noticed.
#
# A COMPANY API KEY IS ENOUGH. If the payments route answers "You are not
# authorized", the cause is almost certainly the API VERSION and not a missing
# permission: /api/v1/payments refuses a company key whatever permissions it
# carries, and /api/v2/payments answers the same key with the data. This file
# used to blame the payment:basic:read scope; that diagnosis was wrong and cost
# a day. The tell is the wording — Whop NAMES a missing scope when one is
# actually missing ("Actor is missing all required permissions: ..."), so a
# generic refusal means a wrong route.
#
# Only read when the dashboard reports in USD — Whop settles in dollars, and
# feeding those into a dashboard reporting in another currency would silently
# mix currencies. A non-USD install keeps the tracker figure instead.
WHOP_API_KEY=

# The client's business day, as an IANA zone name — e.g. America/New_York,
# Europe/London. On a deployment that resolves clients through the registry this
# comes from `clients.time_zone` instead and these variables are not read;
# CLIENT_TIME_ZONE wins over WHOP_TIME_ZONE, which is kept because the KPI
# dashboard has always read that name.
#
# NOT a display preference. It is the answer to "when does a day start for this
# business", and everything that turns an instant into a day has to give the
# same answer, or one screen counts money on one calendar and calls on another.
# The calendar grid is the only thing here that reads it today; the KPI
# dashboard reads the same field, so a deployment that sets one sets both.
#
# Use an IANA name, never a literal "EST" — a fixed offset does not follow
# daylight saving and is an hour out for half the year.
#
# Left blank, the calendar is drawn in UTC and says so on the panel. It will
# not guess: the Calendly account's own zone belongs to whoever created the
# login, and the browser's belongs to whoever is looking.
CLIENT_TIME_ZONE=
WHOP_TIME_ZONE=
